#/bin/bash

set -e

if [ "$1" = "remove" ] ; then
	#echo -n "deleting port redirection from TCP port 80 to TCP port 8080: "
	#iptables -t nat -D PREROUTING 1
	#echo "done"
	echo "no port redirection to delete, no support in kernel"
fi

exit 0
